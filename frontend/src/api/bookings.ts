import { api } from './client'; import type { Booking } from './types';
export type CreateBookingPayload={tableId:string;fullName:string;phone:string;bookingDate:string;bookingTime:string;guestsCount:number;wishes?:string};
export const bookingsApi={ create:(b:CreateBookingPayload)=>api.post<{message:string;bookingId:string;status:string}>('/bookings',b), getToday:()=>api.get<Booking[]>('/bookings/today'), approve:(id:string)=>api.patch<{message:string}>(`/bookings/${id}/approve`), reject:(id:string)=>api.patch<{message:string}>(`/bookings/${id}/reject`), cancel:(id:string)=>api.patch<{message:string}>(`/bookings/${id}/cancel`), checkIn:(id:string)=>api.patch<{message:string}>(`/bookings/${id}/check-in`), complete:(id:string)=>api.patch<{message:string}>(`/bookings/${id}/complete`) };
