name: Apply iPhone admin phone-link patch

on:
  push:
    branches:
      - fix/iphone-admin-phone-link

permissions:
  contents: write

jobs:
  apply:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: fix/iphone-admin-phone-link
          fetch-depth: 0

      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name: Apply exact scoped patch
        run: python .github/scripts/apply_iphone_admin_phone_link.py

      - name: Remove temporary patch tooling
        run: |
          rm -f frontend/test/iphone-admin-phone-link.todo.md
          rm -f .github/scripts/apply_iphone_admin_phone_link.py
          rm -f .github/workflows/apply-iphone-admin-phone-link.yml

      - name: Install frontend dependencies
        run: npm --prefix frontend ci

      - name: Run frontend tests
        run: npm --prefix frontend test

      - name: Build frontend
        run: npm --prefix frontend run build

      - name: Check diff whitespace
        run: git diff --check

      - name: Commit scoped fix
        shell: bash
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add -A
          git commit -m 'fix: use native admin phone links on iPhone'
          git show --check HEAD
          git push origin HEAD:fix/iphone-admin-phone-link
